Об'єкт - це контейнер який складається з:

1. Даних і стану: Атрибути. Адреса(id), значення(mutable or immutable) та тип(type).
2. Поведінка: Методи.





!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
1. Метод створюється всередині класу.
2. Метод те ж саме, що й функція, але завжди прив'язаний до об'єкта, а функція ні.
3. При виклику метода той об'єкт з яким він пов'язаний буде автоматично запускатися в аргументі функції